<div class="controls dashboard-controls">
<?php if (! $this->compact): ?>
<?= $this->tabs ?>
<?php endif ?>
    <div class="sort-controls-container">
        <div class="dashboard-visibility-manager">
            <button
                type="button"
                class="button-link js-dashboard-visibility-toggle"
                title="<?= $this->escape($this->translate('Choose which dashboards are visible in this view')) ?>"
                aria-expanded="false"
            >
                <?= $this->escape($this->translate('Manage Dashboards')) ?>
            </button>
            <div class="dashboard-visibility-panel js-dashboard-visibility-panel" hidden>
                <div class="dashboard-visibility-title">
                    <?= $this->escape($this->translate('Visible Dashboards')) ?>
                </div>
                <ul class="dashboard-visibility-list js-dashboard-visibility-list"></ul>
            </div>
        </div>
        <button
            type="button"
            class="button-link js-toggle-dashlet-view"
            data-label-classic="<?= $this->escape($this->translate('Classic View')) ?>"
            data-label-containers="<?= $this->escape($this->translate('Container View')) ?>"
            data-label-unavailable="<?= $this->escape($this->translate('Container view unavailable in compact layout')) ?>"
            title="<?= $this->escape($this->translate('Switch between classic and container view')) ?>"
            aria-label="<?= $this->escape($this->translate('Switch between classic and container view')) ?>"
        >
            <?= $this->escape($this->translate('Classic View')) ?>
        </button>
        <button
            type="button"
            class="button-link js-reset-dashlet-layout"
            title="<?= $this->escape($this->translate('Reset dashboard layout to default arrangement')) ?>"
            aria-label="<?= $this->escape($this->translate('Reset dashboard layout')) ?>"
        >
            <?= $this->escape($this->translate('Reset Layout')) ?>
        </button>
    </div>
    <p class="dashboard-controls-hint">
        <?= $this->escape($this->translate('Tip: use "Manage Dashboards" to show or hide dashboard tabs for this view.')) ?>
    </p>
</div>
<?php if ($this->dashboard): ?>
    <div class="dashboard content floating-dashlets">
    <?= $this->dashboard ?>
    </div>
<?php else: ?>
    <div class="content">
        <h1><?= $this->escape($this->translate('Welcome to Icinga Web!')) ?></h1>
        <p>
        <?php if (! $this->hasPermission('config/modules')) {
            echo $this->escape($this->translate(
                'Currently there is no dashlet available. Please contact the administrator.'
            ));
        } else {
            printf(
                $this->escape($this->translate('Currently there is no dashlet available. This might change once you enabled some of the available %s.')),
                $this->qlink($this->translate('modules'), 'config/modules')
            );
        } ?>
        </p>
    </div>
<?php endif ?>
