{
  "config": {
    "platform": {
      "php": "8.2"
    }
  },
  "scripts": {
    "assets": "bash ./bin/build-modernui-assets.sh",
    "lint:php": "phpcs --standard=phpcs.xml",
    "stan": "phpstan analyse -c phpstan.neon",
    "stan:baseline:guard": "php ./bin/check-phpstan-baseline-budget.php",
    "test": "phpunit -c phpunit.xml",
    "quality": [
      "@lint:php",
      "@assets",
      "@stan:baseline:guard",
      "@stan",
      "@test"
    ]
  },
  "require-dev": {
    "mockery/mockery": "^1.6"
  }
}
